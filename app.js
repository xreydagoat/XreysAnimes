const shows=[{title:"Azumanga Daioh",type:"Series",genre:"Comedy, Slice of Life",poster:"https://cdn.myanimelist.net/images/anime/1066/117358l.jpg",backdrop:"url('https://cdn.myanimelist.net/images/anime/1066/117358l.jpg')",description:"Azumanga Daioh follows a wonderfully odd class through everyday school life, where ordinary lessons turn into deadpan jokes, strange daydreams, chaotic friendships, and tiny moments that somehow become unforgettable.",episodes:Array.from({length:10},(_,i)=>({label:`Episode ${i+1}`,url:""}))}];let selectedShow=null,activeFilter="all",currentVideo=null,currentAutoSkip=null,currentBlobUrl=null;const hero=document.getElementById("hero"),heroCover=document.getElementById("heroCover"),heroTitle=document.getElementById("heroTitle"),heroDescription=document.getElementById("heroDescription"),heroStatus=document.getElementById("heroStatus"),watchHero=document.getElementById("watchHero"),animeGrid=document.getElementById("animeGrid"),emptyState=document.getElementById("emptyState"),resultCount=document.getElementById("resultCount"),sectionTitle=document.getElementById("sectionTitle"),searchInput=document.getElementById("searchInput"),playerModal=document.getElementById("playerModal"),playerTitle=document.getElementById("playerTitle"),playerMeta=document.getElementById("playerMeta"),videoFrame=document.getElementById("videoFrame"),episodeRow=document.getElementById("episodeRow"),backSkip=document.getElementById("backSkip"),forwardSkip=document.getElementById("forwardSkip"),autoSkipToggle=document.getElementById("autoSkipToggle");const skipWindows={intro:{start:0,end:88},outro:{start:1322,end:1418}};function showHero(show){selectedShow=show;hero.classList.toggle("has-show",Boolean(show));if(!show){heroTitle.textContent="Xreys Animes";heroDescription.textContent="Browse your anime library in Xreys Animes.";heroStatus.textContent="Library";heroCover.removeAttribute("src");heroCover.alt="";watchHero.disabled=true;watchHero.innerHTML='<i data-lucide="play"></i>Nothing loaded';lucide.createIcons();return}hero.style.setProperty("--hero-image",show.backdrop);heroCover.src=show.poster;heroCover.alt=`${show.title} cover`;heroTitle.textContent=show.title;heroDescription.textContent=show.description;heroStatus.textContent=`${show.episodes.length} episodes listed`;watchHero.disabled=false;watchHero.innerHTML='<i data-lucide="play"></i>Watch now';lucide.createIcons()}function filteredShows(){const query=searchInput.value.trim().toLowerCase();return shows.filter(show=>{const matchesFilter=activeFilter==="all"||activeFilter==="watching"||show.type===activeFilter;const matchesSearch=[show.title,show.genre].join(" ").toLowerCase().includes(query);return matchesFilter&&matchesSearch})}function renderCards(){const list=filteredShows();animeGrid.innerHTML="";resultCount.textContent=`${list.length} show${list.length===1?"":"s"}`;sectionTitle.textContent=activeFilter==="Movie"?"Movies":activeFilter==="Series"?"Series":"Shows";emptyState.hidden=list.length>0;list.forEach(show=>{const card=document.createElement("button");card.className="anime-card";card.innerHTML=`<img class="poster" src="${show.poster}" alt="${show.title} poster" loading="lazy"><span class="card-body"><h3>${show.title}</h3><span class="rating">${show.episodes.length} episodes</span><span class="genre">${show.genre||show.type}</span></span>`;card.addEventListener("click",()=>showHero(show));animeGrid.appendChild(card)})}function normalizeVideo(url){if(url.includes("watch?v="))return url.replace("watch?v=","embed/");if(url.includes("youtu.be/"))return`https://www.youtube-nocookie.com/embed/${url.split("youtu.be/")[1].split("?")[0]}`;return url}async function playEpisode(show,episode,index){const url=normalizeVideo(episode.url);playerTitle.textContent=show.title;playerMeta.textContent=episode.label;[...episodeRow.children].forEach((button,buttonIndex)=>button.classList.toggle("active",buttonIndex===index));if(!url){releaseCurrentBlob();videoFrame.innerHTML='<div class="video-loading">Episode preview is not available.</div>';bindCurrentVideo();return}if(!url.match(/\.(mp4|webm|ogg|mkv)(\?.*)?$/i)){releaseCurrentBlob();videoFrame.innerHTML=`<iframe src="${url}" title="${show.title} ${episode.label}" allow="autoplay; encrypted-media; picture-in-picture" allowfullscreen></iframe>`;bindCurrentVideo();return}releaseCurrentBlob();videoFrame.innerHTML='<div class="video-loading">Loading episode...</div>';try{const response=await fetch(url);if(!response.ok)throw new Error(`Could not load ${episode.label}`);const blob=await response.blob();currentBlobUrl=URL.createObjectURL(blob);videoFrame.innerHTML=`<video src="${currentBlobUrl}" controls autoplay controlsList="nodownload noplaybackrate" disablePictureInPicture oncontextmenu="return false"></video>`;bindCurrentVideo()}catch(error){videoFrame.innerHTML='<div class="video-loading">Episode could not load.</div>';console.error(error)}}function bindCurrentVideo(){if(currentVideo&&currentAutoSkip)currentVideo.removeEventListener("timeupdate",currentAutoSkip);currentVideo=videoFrame.querySelector("video");if(!currentVideo)return;currentAutoSkip=()=>{if(!autoSkipToggle.checked)return;const time=currentVideo.currentTime;if(time>=skipWindows.intro.start&&time<skipWindows.intro.end)currentVideo.currentTime=skipWindows.intro.end;if(time>=skipWindows.outro.start&&time<skipWindows.outro.end)currentVideo.currentTime=skipWindows.outro.end};currentVideo.addEventListener("timeupdate",currentAutoSkip)}function skipBy(seconds){if(!currentVideo)return;const duration=Number.isFinite(currentVideo.duration)?currentVideo.duration:currentVideo.currentTime+seconds;const nextTime=Math.min(Math.max(currentVideo.currentTime+seconds,0),duration);if(typeof currentVideo.fastSeek==="function")currentVideo.fastSeek(nextTime);else currentVideo.currentTime=nextTime}function releaseCurrentBlob(){if(currentBlobUrl){URL.revokeObjectURL(currentBlobUrl);currentBlobUrl=null}}function openPlayer(show){if(!show)return;episodeRow.innerHTML="";show.episodes.forEach((episode,index)=>{const button=document.createElement("button");button.textContent=episode.label;button.addEventListener("click",()=>playEpisode(show,episode,index));episodeRow.appendChild(button)});playerModal.showModal();playEpisode(show,show.episodes[0],0)}function setFilter(filter){activeFilter=filter;document.querySelectorAll("[data-filter]").forEach(button=>button.classList.toggle("active",button.dataset.filter===filter));renderCards()}document.querySelectorAll("[data-filter]").forEach(button=>button.addEventListener("click",()=>setFilter(button.dataset.filter)));watchHero.addEventListener("click",()=>openPlayer(selectedShow));document.getElementById("closePlayer").addEventListener("click",()=>{videoFrame.innerHTML="";currentVideo=null;currentAutoSkip=null;releaseCurrentBlob();playerModal.close()});backSkip.addEventListener("click",()=>skipBy(-10));forwardSkip.addEventListener("click",()=>skipBy(10));searchInput.addEventListener("input",renderCards);showHero(shows[0]||null);renderCards();lucide.createIcons();
